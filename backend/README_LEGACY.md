# Backend Node.js (legacy)

Este directorio contenía el backend original en **Node.js (Express + xlsx)**.

El backend en uso es **Python** (FastAPI + pandas). Ver **`../backend_python/`** y el `docker-compose.yml` del proyecto, que construye el servicio `backend` desde `backend_python`.

Se mantiene este código como referencia; no se usa en el despliegue actual.
